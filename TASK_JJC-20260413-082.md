# JJC-20260413-082: PDF 元数据编辑

## 需求
在工作流面板中新增"📝 编辑元数据"步骤，支持设置 PDF 的标题、作者、主题、关键词。

## 文件清单

### 1. `src/modules/pipeline.js` — 新增 metadata 步骤处理函数

在文件末尾 `STEP_HANDLERS` 之前，新增 `stepMetadata` 函数：

```javascript
/**
 * metadata — 编辑 PDF 元数据（标题、作者、主题、关键词）
 * 使用 pdf-lib 原生 API：setTitle() / setAuthor() / setSubject() / setKeywords()
 */
async function stepMetadata(inputBytes, options = {}) {
  const pdfDoc = await PDFLib.PDFDocument.load(inputBytes)

  const { title, author, subject, keywords } = options

  if (title && title.trim()) {
    pdfDoc.setTitle(title.trim())
  }
  if (author && author.trim()) {
    pdfDoc.setAuthor(author.trim())
  }
  if (subject && subject.trim()) {
    pdfDoc.setSubject(subject.trim())
  }
  if (keywords && keywords.trim()) {
    // keywords 是逗号分隔的字符串，转为数组
    const kwArray = keywords.split(',').map(k => k.trim()).filter(k => k.length > 0)
    if (kwArray.length > 0) {
      pdfDoc.setKeywords(kwArray)
    }
  }

  const outputBytes = await pdfDoc.save({
    useObjectStreams: true,
    addDefaultPage: false,
    objectsPerTick: 50,
  })

  return outputBytes
}
```

然后在 `STEP_HANDLERS` 对象中注册：

```javascript
const STEP_HANDLERS = {
  compress: stepCompress,
  watermark: stepWatermark,
  stripJS: stepStripJS,
  metadata: stepMetadata,  // ← 新增
}
```

### 2. `index.html` — 新增 UI 元素

在 Strip JS 步骤后面（约第720行），添加：

#### 复选框（在 `#pipeline-steps` div 内）：
```html
<!-- Metadata Step -->
<label class="pipeline-step-item">
  <input type="checkbox" class="pipeline-step-check" data-step="metadata">
  <div class="pipeline-step-content">
    <span class="pipeline-step-icon">📝</span>
    <span class="pipeline-step-label">编辑元数据</span>
  </div>
</label>
```

#### 选项区（在水印选项区之后，Actions 之前）：
```html
<!-- Metadata Options (shown when metadata checkbox checked) -->
<div class="pipeline-metadata-options" id="pipeline-md-options" style="display:none;">
  <div class="wm-option-group">
    <label class="wm-option-label" for="pipeline-md-title">标题：</label>
    <input type="text" id="pipeline-md-title" class="wm-text-input" placeholder="PDF 标题" maxlength="200">
  </div>
  <div class="wm-option-group">
    <label class="wm-option-label" for="pipeline-md-author">作者：</label>
    <input type="text" id="pipeline-md-author" class="wm-text-input" placeholder="作者/公司名称" maxlength="200">
  </div>
  <div class="wm-option-group">
    <label class="wm-option-label" for="pipeline-md-subject">主题：</label>
    <input type="text" id="pipeline-md-subject" class="wm-text-input" placeholder="文档主题" maxlength="200">
  </div>
  <div class="wm-option-group">
    <label class="wm-option-label" for="pipeline-md-keywords">关键词：</label>
    <input type="text" id="pipeline-md-keywords" class="wm-text-input" placeholder="关键词，用逗号分隔" maxlength="500">
  </div>
</div>
```

### 3. `src/modules/pipeline-ui.js` — 集成 metadata 步骤

#### 3.1 模板定义更新
在 `TEMPLATES` 对象中，修改 `report` 模板：
```javascript
report: {
  steps: ['compress', 'metadata'],
  watermark: null,
  metadata: { author: '公司名称' },  // 财务场景默认作者
},
```

#### 3.2 `applyTemplateData` 函数 — 填充 metadata 参数
在现有 watermark 参数填充逻辑之后，添加：
```javascript
// Fill metadata parameters
if (tpl.metadata) {
  const mdTitle = document.getElementById('pipeline-md-title')
  const mdAuthor = document.getElementById('pipeline-md-author')
  const mdSubject = document.getElementById('pipeline-md-subject')
  const mdKeywords = document.getElementById('pipeline-md-keywords')
  if (mdTitle) mdTitle.value = tpl.metadata.title || ''
  if (mdAuthor) mdAuthor.value = tpl.metadata.author || ''
  if (mdSubject) mdSubject.value = tpl.metadata.subject || ''
  if (mdKeywords) mdKeywords.value = tpl.metadata.keywords || ''
}
```

#### 3.3 新增 metadata 选项显示/隐藏逻辑
```javascript
function updateMdOptionsVisibility() {
  const mdOptions = document.getElementById('pipeline-md-options')
  const mdCheckbox = document.querySelector('.pipeline-step-check[data-step="metadata"]')
  if (mdOptions && mdCheckbox) {
    mdOptions.style.display = mdCheckbox.checked ? 'block' : 'none'
  }
}
```

#### 3.4 `buildSteps` 函数 — 新增 metadata 步骤
在 `stripJS` 的 else if 之后添加：
```javascript
} else if (stepType === 'metadata') {
  const title = document.getElementById('pipeline-md-title').value.trim()
  const author = document.getElementById('pipeline-md-author').value.trim()
  const subject = document.getElementById('pipeline-md-subject').value.trim()
  const keywords = document.getElementById('pipeline-md-keywords').value.trim()

  // 至少填一个字段
  if (!title && !author && !subject && !keywords) {
    showError(statusEl, '⚠️ 请至少填写一个元数据字段')
    return null
  }

  steps.push({
    type: 'metadata',
    title,
    author,
    subject,
    keywords,
  })
}
```

#### 3.5 事件绑定
在 step checkbox change 监听中添加 `updateMdOptionsVisibility()` 调用：
```javascript
document.querySelectorAll('.pipeline-step-check').forEach(cb => {
  cb.addEventListener('change', () => {
    updateWmOptionsVisibility()
    updateMdOptionsVisibility()  // ← 新增
    updateBatchRunState()
  })
})
```

#### 3.6 `handleSaveTemplate` 函数 — 保存 metadata
在现有的 watermark 和 stripJS 处理逻辑之后，添加 metadata 处理：
```javascript
} else if (stepType === 'metadata') {
  const title = document.getElementById('pipeline-md-title').value.trim()
  const author = document.getElementById('pipeline-md-author').value.trim()
  const subject = document.getElementById('pipeline-md-subject').value.trim()
  const keywords = document.getElementById('pipeline-md-keywords').value.trim()
  steps.push({
    type: 'metadata',
    title, author, subject, keywords,
  })
}
```

以及 metadata 数据序列化（在 watermarkData 之后）：
```javascript
const mdStep = steps.find(s => s.type === 'metadata')
const metadataData = mdStep
  ? { title: mdStep.title, author: mdStep.author, subject: mdStep.subject, keywords: mdStep.keywords }
  : null

const data = { steps: stepTypes, watermark: watermarkData, metadata: metadataData }
```

#### 3.7 `applyTemplateData` — 显示/隐藏 metadata 选项
在 `applyTemplateData` 最后添加：
```javascript
// Show metadata options if metadata step is selected
updateMdOptionsVisibility()
```

#### 3.8 进度显示 — 添加 metadata 步骤名称映射
在 `runSingle` 和 `runBatch` 中的 stepNames 对象中，添加 `'metadata': '元数据'`。

## 约束
- 纯 JS，不引入新依赖（pdf-lib 已有）
- 使用 pdf-lib 原生 API
- 复用已有的 CSS 类（wm-option-group, wm-option-label, wm-text-input）

## 完成步骤
1. 修改以上 3 个文件
2. `npm run build`
3. 复制 `dist/index.html` 到 `docs/index.html`（如果 build 配置不是直接输出到 docs）
4. `git config --global https.proxy http://127.0.0.1:7897`
5. `git add . && git commit -m "feat: add PDF metadata editing step (JJC-20260413-082)" && git push`
